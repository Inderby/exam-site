

dependencies {
    implementation("$boot:spring-boot-starter-data-jpa")

    testRuntimeOnly("com.h2database:h2")

    compileOnly(project(":comp-paper-user"))
    testCompileOnly(project(":comp-paper-user").sourceSets.test.output)
}

